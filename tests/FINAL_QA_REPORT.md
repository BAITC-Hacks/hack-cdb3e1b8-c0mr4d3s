# Финальная проверка объединённого решения

23 сентября 2026. Приложение взято из main `60879fb53477ca6d30baa0449427ef6613381bf9`; QA объединён в ветке `qa/tests` поверх `cfe6e32`. Все тесты находятся в tests/. main не изменялся.

## Результат

**69 из 69 тестов прошли одним unittest discovery.** Кроме них успешно выполнены оценочные прогоны, проверки лимитов, экспорт CSV, сценарии сбоев Gemini и локальный запуск бота с SQLite. Python 3.12.14, Windows, 32 зафиксированные зависимости, pip check без ошибок.

| Проверка | Результат |
|---|---|
| Единый unittest discovery | 69/69 PASS, без двойного подсчёта |
| Корневой local_eval.py: один прогон | PASS |
| Корневой local_eval.py: seed=0–9 | 10/10 без ошибок, все в плюс |
| Строгий QA: seed=42 и 0–9 | 11/11, кампании и ресурсы корректны |
| run_demo | Один Agent.act и один исходный score_campaigns; пилоты включены |
| Два корневых make_submission.py | Одинаковый CSV из 10 кампаний |
| Пакетный scripts.local_eval --submission | CSV совпадает с корневым экспортом |
| Gemini offline / timeout / invalid_json / invalid_ids | PASS без обращения к внешнему API |
| Бот --once --backend real | Настоящий агент, результат сохранён в SQLite |
| Ошибки SQLite и новый процесс | Ошибки не выдаются за успех; сохранённый результат восстанавливается |
| Корневой импорт Agent вне папки проекта | Работает без импорта Telegram и SQLite |

## Проверка именно на исходном комплекте организаторов

В отдельную временную папку скопированы исходные local_eval.py, make_submission.py, environment.py, mock_environment.py, scoring_core.py и данные организаторов. Эти файлы не редактировались; их SHA-256 сохранены в FINAL_RESULTS.json.

К ним добавлены только корневой agent.py и необходимые модули scripts/__init__.py, scripts/agent.py, scripts/config.py. Модулей бота в этом проверочном комплекте нет.

Исходные команды организаторов успешно выполнили один прогон, десять прогонов и два экспорта. Экспорты совпали друг с другом и с CSV объединённого решения. Таким образом, ранее выявленное расхождение корневого импорта и команд исправлено и проверено на предоставленной мок-среде.

Передавайте scripts/ и данные вместе с корневым agent.py: сам по себе файл-вход не содержит всю реализацию. Скрытая среда судей недоступна, её результаты не заявляются.

## Контрольные значения

Seed=42: net_arpu_gain **3 329 109,816487866**, total_cost **52 876**, total_contacts **13 219**, **12 пилотов**, **10 финальных кампаний**. Расходы и контакты включают пилоты.

CSV SHA-256 в проверенной Windows-среде:

```text
559dc9b2673b7556d54e04d817baa1f8c4fcf008b23880feed4ab9c490ce96e9
```

Значения эффектов в мок-среде не предсказывают балл на судействе. Статус PASS скоринга — положительный net_arpu_gain; соответствие лимитам и интерфейсам дополнительно проверено тестами.

## Что изменено при объединении

- Приложение и данные из main включены в qa/tests. Стратегия, бот, среда и scoring_core сохранены без изменений относительно проверенного main.
- QA перенесён в tests/ и включён в общий discovery; старые qa/ и demo_eval.py убраны из текущего дерева, история Git сохранена.
- Добавлены тонкие корневые точки входа agent.py, local_eval.py, make_submission.py. Они используют существующий пакет scripts/; второй формулы оценки нет.
- Включены корневой submission.csv и пакетный data/submission.csv.
- Переписан основной README: установка, сдача, алгоритм, тесты, демонстрация, Telegram и ограничения.

## Последнее действие перед защитой

Владелец живого бота обновляет эту версию, сохраняет свою локальную конфигурацию и проверяет `/run` → `/last` → `/history`. В финальном автономном QA сетевые Telegram-вызовы заменены тестовыми объектами: живой процесс и SQLite на компьютере участника не подключались. Фиксированный seed=42 для показа допустим и воспроизводим.

Итог: **локальная интеграция и совместимость с предоставленным комплектом — PASS; живой Telegram-показ — отдельная ручная проверка**. Перед публичной демонстрацией не требуется повторять весь QA, если код и конфигурация не менялись.
