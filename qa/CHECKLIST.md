# QA: проверено и осталось

Актуальная проверенная основа: `main` на
`19a913086d840616f7deeebebf5b0a9d9187381f` с модулем `demo_eval.py` из QA-ветки.
Требования: руководства участников, полный PDF кейса и согласованный план команды.
Обещанное упрощение репозитория потребует проверки нового коммита.

## Агент и установка

- [x] Корректный самостоятельный `Agent.act`, 1–10 финальных кампаний.
- [x] Пилоты, влияние их наблюдений на решения, учёт бюджета/контактов.
- [x] Стандартные лимиты, границы размера пилота 10/200, до 20 пилотов.
- [x] Отрицательные/нечисловые наблюдения и ошибка одного пилота.
- [x] Чистая установка закреплённых зависимостей и `pip check`.
- [x] 11 seed без ошибок, два одинаковых экспорта CSV в offline-режиме.
- [x] Исторически проверены Gemini и его fallback; см. отдельный [отчёт](GEMINI_TEST_REPORT.md).

## Eval и локальная интеграция

- [x] Пользователь взял ответственность за `demo_eval.py/run_demo`.
- [x] Один вызов агента и одна среда на `run_demo`.
- [x] Исходный скоринг, точные `explicit_ids` пилотов плюс финальные кампании.
- [x] Передача seed, повторяемость при одинаковом seed, изменение выборок при другом.
- [x] Совпадение метрик с официальным оценщиком на нескольких seed.
- [x] Результат сериализуется в строгий JSON на проверенных сценариях.
- [x] При падении агента сохраняется оценка проведённых пилотов и безопасное поле ошибки.
- [x] Реальный бот вызывает новый `run_demo`, сохраняет результат во временную SQLite.
- [x] `/last` и `/history` восстанавливают его без повторного вызова агента.
- [x] Девять новых тестов; всего с остальными наборами 56 успешных тестов.

## Бот: автоматические проверки с моками Telegram

| Сценарий | Состояние |
|---|---|
| `/run`, сохранение, `/last`, `/history` | Проверено |
| Последние пять записей, порядок по ID | Проверено |
| Раздельная история разных чатов | Проверено |
| Пустая база | Проверено |
| Ошибка eval и следующий успешный запуск | Проверено |
| Повторное открытие SQLite, восстановление прерванного запуска | Проверено |
| Второй одновременный `/run`, чтение истории во время расчёта | Проверено |
| Сбой отправки результата в Telegram после записи | Запись сохраняется |
| Неверный токен, сбой сети и отсутствие токена в диагностике | Проверено на моках |
| Ошибка записи SQLite / заполненный диск | Нужна отдельная проверка |
| Жёсткий таймаут всего зависшего агента | Не реализован в текущем боте |

## Действия владельца бота

- [ ] Получить eval-модуль из QA-ветки по [инструкции](DEMO_INTEGRATION.md).
- [ ] Убедиться, что запущенная версия приложения соответствует проверенной.
- [ ] Переключить `bot.runner_module` на `demo_eval`, сохранив остальные настройки.
- [ ] Прогнать offline-проверку через отдельную тестовую базу.
- [ ] Перезапустить единственный процесс рабочего бота.
- [ ] Показать `/run` → `/last` → `/history` → перезапуск → `/last` и сверить результат.
- [ ] После принятия модуля согласованно убрать дублирование с `bot_eval.py`.

Эти действия на компьютере участника ещё не выполнены нами. Наличие сообщения
`PASS` в Telegram само по себе не подтверждает весь список.

## Перед окончательной сдачей

- [ ] После упрощения приложения записать новый SHA и повторить затронутые проверки.
- [ ] Штатные `local_eval.py`, `local_eval.py --runs 10`, `make_submission.py` на окончательной версии.
- [ ] Согласованный режим LLM и воспроизводимый итоговый `submission.csv`.
- [ ] Дополнительные модули/данные принимаются правилами сдачи; чистая копия запускается.
- [ ] README приложения содержит окончательные команды, зависимости и режим запуска.
- [ ] Проверка совместимости включённого LLM с судейством или явный offline-режим.
- [ ] Окончательный показ живого бота и сохранённой истории.

Критерии защиты в полном PDF: работоспособность 25, техническая реализация 25,
README/воспроизводимость 25, ценность 15, оригинальность/развитие 10.
Локальный `net_arpu_gain` не является прогнозом этих баллов.
Публикация QA-ветки, переключение бота и слияние в `main` — отдельные действия.
