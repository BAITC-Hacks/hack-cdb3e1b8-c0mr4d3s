# Стартовая проверка — 23 сентября 2026

Проверен исходный `agent_template.py` из пакета участников, скопированный
в `agent.py` только в изолированной рабочей папке. Это НЕ агент команды.
Код оценки, мок-среда и данные организаторов не изменялись.
Среда: Python 3.12.14, pandas 3.0.1, NumPy 2.3.5.

## Найдено

| Приоритет | Сценарий | Результат |
|---|---|---|
| Высокий | Шаблон, seed=3 | 6 пилотов, 0 финальных кампаний. Нарушено требование 1–10 кампаний |
| Средний | Шаблон, seed=2 | 4 финальные кампании; кампании с индексами 1 и 3 имеют одинаковый сегмент. Нарушен договор команды |
| Информация | `local_eval.py` | Ловит исключение из `Agent.act`, отбрасывает некорректные кампании и обрезает список. Код выхода 0 сам по себе не подтверждает корректность агента |
| Информация | `scoring_core.py` | Автоматически ограничивает бюджет и охват. QA дополнительно показывает, когда план полагается на такую обрезку |

## Выполнено

- Исходный `python local_eval.py --runs 10`: завершён без падения процесса,
  но чистый результат отрицателен во всех 10 прогонах.
- Медиана: −357 948; минимум: −1 019 431; максимум: −76 493 у.е.
- Дополнительная строгая проверка seed 42 и 0–9: обнаружила обе ошибки выше.
- Seed 42: net −1 035 279,45; стоимость 22 280; контакты 5570; 6 пилотов;
  2 финальные кампании; экономический статус исходного скоринга FAIL.
- Два независимых вызова `make_submission.build_submission` с seed 42
  дали одинаковый CSV из двух кампаний.
- SHA-256 нормализованного CSV (UTF-8, LF):
  `2111f50e6c8322d56609bd835257c89c371cb4a26eebbc61543294141f0e807b`.

## Ещё не проверено

В командном репозитории на момент чтения веток был только README в `main`
(коммит `6e0de967841ffede526e7730da2e4ce38d25ab8d`). Реальные `agent.py`,
`demo_eval.py` и `bot.py` пока отсутствуют. Поэтому нельзя подтвердить
готовность командной сдачи, команды Telegram или сохранение в SQLite.

Тесты `test_qa_check.py` используют искусственные ошибочные реализации
для проверки самого QA-инструмента. Они не подменяют тестирование будущего бота.
Баллы мок-среды не являются прогнозом результатов судейства.
